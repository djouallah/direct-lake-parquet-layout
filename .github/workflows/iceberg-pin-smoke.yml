name: Iceberg pin smoke

# A DUMMY CI. It builds nothing, commits nothing, and spends no Fabric build capacity — it installs
# the EXACT DuckDB wheel the iceberg leg pins (`fabric_run.py`'s `duckdb==`, read out of that file,
# never restated here) and asks three questions about it that a release cannot answer:
#
#   1. does the iceberg extension still load and still talk to the OneLake Iceberg REST catalog,
#   2. does the parquet that build writes — locally AND through the iceberg writer — carry
#      `ColumnMetaData.encoding_stats`, and
#   3. does an Iceberg TABLE PROPERTY still set the row group size, i.e. does `iceberg_geometry()`
#      still bind.
#
# **WHY (2) IS THE POINT.** duckdb/duckdb#24957 (`9d360460a4`, 2026-08-24) makes the Parquet writer
# emit `encoding_stats` / `PageEncodingStats`. That list is the only footer structure letting a
# reader prove a chunk is entirely dictionary-encoded WITHOUT decoding pages, and Direct Lake uses
# it: the PR measures a cold first-touch of a 142M-row dictionary string column going
# **10,857.5 ms -> 689.3 ms**. CLAUDE.md records iceberg as the one writer here that emits no RLE
# and leaves 10 of 53 chunks with no dictionary page at all, which is exactly the transcode this
# fixes.
#
# ⚠️ **THIS RAN THE `main` CLI UNTIL 2026-09-13 AND DOES NOT ANY MORE. DO NOT PUT IT BACK.** It
# fetched `artifacts.duckdb.org/latest/duckdb-cli-linux-amd64.tar.gz` because #24957 was in no
# release and the WHEEL lagged — `1.6.0.dev365` carried a core 102 commits before the fix, so
# `encoding_stats` read absent there and a main CLI was the only place the fix existed at all. That
# was a WORKAROUND and it has expired: the pinned wheel carries the fix (measured on `1.6.0.dev379`,
# core `v2.0.0-alpha39998`, and on the current pin).
#
# **What the workaround then cost is the reason for this warning.** The CLI is built from `main` and
# the wheel is built from the v2.0 RELEASE branch (duckdb-python pins "the latest cyanoptera hash"),
# so the two are DIFFERENT BRANCHES with independent alpha counters — `v2.1.0-alpha41532` against
# `v2.0.0-alpha41344` on the same day. Two false alarms came out of reading them as one number line:
# TODO.md carried a *do not move the pin* entry built on a `v2.1.0-alpha40144` CTAS assertion that
# could not describe the wheel, and run 34738321320 went red on `INSTALL iceberg` 404ing for a main
# version with no published extension, which skipped the token steps and killed the probe that
# actually mattered. **A smoke test for the leg runs what the leg runs.**
#
# **IT PROVISIONS ITS OWN LAKEHOUSE AND THE NAME IS LOAD-BEARING** — `provision.py smoke` ensures
# `dbt_duckdb_main_smoke`, never `dbt_iceberg`. `ensure()` REUSES an item it finds by display name
# and `teardown` DELETES by GUID whatever the record names, so borrowing the iceberg leg's item
# would let a smoke dispatch fired next to a hand-dispatched iceberg build share that lakehouse and
# then delete it mid-run. With its own name there is no collision to reason about. **That name still
# says `main` and must not be tidied**: `ensure()` matches on display name, so a new spelling
# creates a SECOND lakehouse and leaves the old one standing and billing.
#
# **DELIBERATELY NOT ON THE `onelake-<ref>` CONCURRENCY GROUP.** That group holds at most ONE
# pending run, so joining it would let this smoke test EVICT a queued `Benchmark` — a free
# 5-minute check displacing an hour of paid capacity. It writes a few million rows into an item
# nothing else touches and reads no table any other workflow owns, so there is nothing to race on.
#
# **`workflow_dispatch` ONLY.** No `push` (this repo's rule is that nothing which commits or spends
# answers a push, and while this commits nothing it does create a Fabric item), no `schedule` (the
# question is "can the pinned wheel still do what the leg needs", which a human asks before and
# after moving the `duckdb==` pin in fabric_run.py, not nightly), no `workflow_run`.

on:
  workflow_dispatch:
    inputs:
      onelake:
        description: "Attach the OneLake Iceberg REST catalog (needs the Fabric secrets)"
        type: boolean
        default: true

permissions:
  id-token: write        # OIDC for azure/login
  contents: read

concurrency:
  # Its OWN group, so two smoke dispatches cannot both ensure/teardown the one lakehouse — and so
  # it never queues behind, or evicts, a Benchmark. See the header.
  group: iceberg-pin-smoke-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      # Job-level, not workflow-level: the `secrets` context is unavailable in a workflow-level
      # `env` block. Same arrangement as benchmark.yml.
      WS_ID: ${{ secrets.FABRIC_WORKSPACE_ID }}
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      # `ensure()` writes the lakehouse GUID here; the teardown step reads it back. Same mechanism
      # as every job in benchmark.yml — no bespoke deletion code anywhere in this file.
      RUN_RECORD: ${{ github.workspace }}/record/record-00-smoke.json
      SMOKE_TABLE: duckdb_main_smoke
      # TWO MORE tables, written with Iceberg table properties, so the row-group probe below cannot
      # take the encoding_stats proof down with it if a property is refused. Same lakehouse, so the
      # teardown removes all three with the one item.
      SMOKE_TABLE_RG: duckdb_main_smoke_rg          # the shipping pair: rows + byte budget
      SMOKE_TABLE_RG_CTL: duckdb_main_smoke_rg_ctl  # the control: rows ALONE
      # ⚠️ THESE NUMBERS ARE SIZED SO THE PROBE CAN FAIL, AND THE OLD ONES WERE NOT.
      # It used to write 2 narrow columns x 1M rows -- about 10 MB -- and set the rows property
      # alone. On a table that small duckdb-iceberg's hardcoded 128 MB byte budget
      # (iceberg_insert.hpp:69) never binds, so a rows-only property DOES work, the probe read
      # "4 row groups, honoured", and the nyc leg then moved nothing (run 33733500776: 729 row
      # groups at 811,700 rows against a baseline 728 at 812,815). The probe was structurally
      # unable to see the one failure it existed to catch.
      #
      # So the table is now WIDE ENOUGH for the byte budget to bind, and the probe writes it twice:
      #   control  (rows only)      -> 128 MB binds -> MANY row groups, the row count ignored
      #   shipping (rows + 1 GiB)   -> the ROW COUNT binds -> exactly TOTAL/ROWS row groups
      # Measured locally through plain COPY (same batch_size fields the properties reach), 20 BIGINT
      # columns x 20M rows asking 5M rows/group: 128 MB gave 25 groups at 827,392 rows, 1 GiB gave
      # 4 at 5,001,216. At the 4M rows below the two read 13 and 4. The control asserts only
      # "more groups than the shipping case", so it stays true if the row width drifts.
      SMOKE_RG_COLS: "50"
      SMOKE_RG_TOTAL: "4000000"
      SMOKE_RG_ROWS: "1000000"
      SMOKE_RG_EXPECTED: "4"
      # MUST EQUAL `row_group_budget_mb` in macros/iceberg_geometry.sql -- test_iceberg_geometry.py
      # asserts they agree, because a probe testing a budget the macro does not emit proves nothing.
      SMOKE_RG_BUDGET_MB: "1024"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}

      # ---------------------------------------------------------------- the binary under test
      # ONE venv on PATH for the whole job, so every step below — `provision.py` included — runs on
      # the same interpreter with the same DuckDB. The pin is READ OUT OF fabric_run.py, never
      # restated here: two copies of a version would drift and this workflow would then measure a
      # wheel the leg does not install.
      #
      # **THE WHEEL'S PyPI VERSION DOES NOT NAME ITS CORE, WHICH IS WHY THIS READS
      # `pragma_version()` RATHER THAN `duckdb.__version__`.** Measured: `1.6.0.dev365` — a
      # `1.6.0.dev` string — carried core `v2.0.0-alpha38615 / 16980de6d3`, and the current
      # `2.0.0.dev<YYMMDDHHMM>` scheme (duckdb-python `77f2551d40`, 2026-09-03) is a BUILD TIMESTAMP
      # naming no core either. Only the pragma answers, and the answer goes in the step summary so a
      # later run can be read against this one.
      - name: Install the wheel the iceberg leg pins
        run: |
          set -euo pipefail
          PIN=$(grep -oE 'duckdb==[0-9][^"]*' .github/scripts/fabric_run.py | head -1)
          echo "PIN=${PIN:?no duckdb pin found in fabric_run.py}" >> "$GITHUB_ENV"
          python -m venv "$RUNNER_TEMP/pin"
          echo "$RUNNER_TEMP/pin/bin" >> "$GITHUB_PATH"
          # `requests` is provision.py's only dependency. An EXACT pre-release specifier resolves
          # without `--pre`; passing it anyway makes the intent legible on a line installing a
          # nightly, and keeps this working if the pin is ever loosened to a range.
          "$RUNNER_TEMP/pin/bin/pip" install -q --pre "$PIN" requests
          V=$("$RUNNER_TEMP/pin/bin/python" - <<'PY'
          import duckdb
          c = duckdb.connect()
          core = c.sql("SELECT library_version || ' / ' || source_id FROM pragma_version()").fetchone()[0]
          print(duckdb.__version__ + "|" + core)
          PY
          )
          echo "DUCKDB_CORE=${V##*|}" >> "$GITHUB_ENV"
          echo "### Iceberg pin smoke" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| check | result |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---|---|" >> "$GITHUB_STEP_SUMMARY"
          echo "| pinned wheel | \`$PIN\` -> \`${V%%|*}\`, core \`${V##*|}\` |" >> "$GITHUB_STEP_SUMMARY"

      # ---------------------------------------------------------------- does iceberg load at all
      - name: INSTALL iceberg
        run: |
          set -euo pipefail
          python - <<'PY'
          import os, duckdb
          c = duckdb.connect()
          c.sql("INSTALL iceberg; INSTALL azure; LOAD iceberg; LOAD azure")
          ext = c.sql("""SELECT string_agg(extension_name || ' ' || extension_version ||
                                           ' (' || installed_from || ')', ', ')
                         FROM duckdb_extensions()
                         WHERE extension_name IN ('iceberg','azure') AND installed""").fetchone()[0]
          print("extensions:", ext)
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a") as fh:
              print(f"| extensions | {ext} |", file=fh)
          PY

      # ---------------------------------------------------------------- the control, no secrets
      # FATAL, and it runs BEFORE anything is provisioned: a pin that cannot emit `encoding_stats`
      # at all must be reported as that, not as a red iceberg-writer step twenty lines later. It
      # proves the WHEEL has the fix independently of anything Fabric-side, so a red step below can
      # only be about the iceberg writer or the catalog. `duid` is low-cardinality and must be
      # dictionary-encoded; `mw` is unique and must not be, which is what makes the assertion a
      # measurement rather than a tautology (it fails on a column with no dictionary).
      #
      # This replaced a `gh api .../compare/<#24957 commit>...<CLI sha>` ancestry check, which only
      # a CLI build has a commit for. Reading the column off the footer is the better question
      # anyway: it asks whether the fix is WORKING, not whether it is nominally in there.
      - name: Locally written parquet carries encoding_stats
        run: |
          set -euo pipefail
          python - <<'PY'
          import os, sys, duckdb
          c = duckdb.connect()
          out = os.path.join(os.environ["RUNNER_TEMP"], "local.parquet")
          c.sql(f"""COPY (SELECT ['AAA','BBB','CCC','DDD'][(i % 4) + 1] AS duid, i AS mw
                          FROM range(2000000) t(i)) TO '{out}' (FORMAT parquet)""")
          cols = {d[0] for d in c.sql(f"SELECT * FROM parquet_metadata('{out}') LIMIT 0").description}
          if "encoding_stats" not in cols:
              print("::error::this wheel's parquet_metadata has no encoding_stats column — the pin "
                    "predates duckdb/duckdb#24957 (9d360460a4)")
              sys.exit(1)
          n = c.sql(f"""SELECT count(*) FROM parquet_metadata('{out}')
                        WHERE path_in_schema = 'duid'
                          AND len(list_filter(encoding_stats,
                                              lambda x: x.page_type = 'DICTIONARY_PAGE')) > 0""").fetchone()[0]
          if n == 0:
              print("::error::encoding_stats carries no DICTIONARY_PAGE for duid")
              sys.exit(1)
          print(f"local: {n} dictionary-encoded chunk(s)")
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a") as fh:
              print("| local parquet encoding_stats | present |", file=fh)
          PY

      # ---------------------------------------------------------------- OneLake
      # ⚠️ ALL THREE ARE `always()`, AND THAT IS WHAT MAKES THE ROW-GROUP PROBE'S OWN `always()`
      # MEAN ANYTHING. They were plain `if: inputs.onelake`, i.e. `success()`, so a red step above
      # skipped the login, the lakehouse and the token — and that probe then died on
      # `KeyError: 'ONELAKE_TOKEN'` having tested nothing (run 34738321320). **A step guarded
      # `always()` is only as reachable as its least reachable input.**
      - uses: azure/login@v2
        if: always() && inputs.onelake
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          allow-no-subscriptions: true

      - name: Provision the smoke lakehouse
        if: always() && inputs.onelake
        run: |
          set -euo pipefail
          # `record.item()` writes the fragment to this path and will not create its directory.
          mkdir -p "$(dirname "$RUN_RECORD")"
          python .github/scripts/provision.py smoke >> "$GITHUB_ENV"

      - name: Mint a OneLake token
        if: always() && inputs.onelake
        run: |
          ST=$(az account get-access-token --resource https://storage.azure.com/ --query accessToken -o tsv)
          echo "::add-mask::$ST"; echo "ONELAKE_TOKEN=$ST" >> "$GITHUB_ENV"

      # The integration proof.
      #
      # The ATTACH options are copied from the `iceberg` target in profiles.yml. Keep them in step:
      # if that target changes, this stops testing what the leg actually does.
      #
      # **`SET azure_transport_option_type = 'curl'` IS REQUIRED ON A BARE RUNNER**, and it appears
      # in every session below that touches OneLake — a setting is per session, so one copy would
      # not cover the others. Without it, run 32788719423 attached fine, CREATED the table fine, and
      # then died READING IT BACK with
      #   Fail to get a new connection for: https://onelake.blob.fabric.microsoft.com.
      #   Problem with the SSL CA cert (path? access rights?)
      # which is the exact string the azure extension's docs name: on Linux the default transport
      # does not locate a CA bundle, and asking for `curl` makes it search the standard paths.
      # `CURL_CA_INFO` / `CURL_CA_PATH` are the env-var escape hatch if a future image defeats that.
      # This never bit the iceberg LEG because that runs in a Fabric notebook, not on ubuntu-latest.
      #
      # ⚠️ **NO `getenv()` ANYWHERE IN THIS FILE.** It is a CLI-only scalar function — absent from
      # the Python API ("Catalog Error: Scalar Function with name getenv does not exist", run
      # 33730952764) — so every secret is interpolated from `os.environ` instead. Nothing here
      # prints a statement or a token, the runner masks the token anyway, and it still reaches no
      # file on disk, which is what the deleted CLI steps used `getenv()` to achieve. Interpolated
      # and NOT bound: `ACCESS_TOKEN ?` raises `Not implemented Error: Unrecognized expression type
      # PARAMETER` (run 33731225546) — CREATE SECRET's option list is not an expression context.
      - name: Attach the OneLake Iceberg REST catalog and round-trip a table
        if: inputs.onelake
        run: |
          set -euo pipefail
          python - <<'PY'
          import os, sys, duckdb
          c = duckdb.connect()
          c.sql("LOAD iceberg; LOAD azure")
          c.sql("SET azure_transport_option_type = 'curl'")
          tok, ws, ep = (os.environ["ONELAKE_TOKEN"], os.environ["WAREHOUSE_PATH"],
                         os.environ["ONELAKE_ENDPOINT"])
          c.execute(f"CREATE SECRET onelake_storage "
                    f"(TYPE azure, PROVIDER access_token, ACCESS_TOKEN '{tok}')")
          c.execute(f"""ATTACH '{ws}' AS onelake (
                          TYPE iceberg, endpoint '{ep}', token '{tok}',
                          access_delegation_mode 'none', stage_create_tables 0,
                          skip_create_table_metadata_updates 1, default_schema 'dbo')""")
          t = os.environ["SMOKE_TABLE"]
          c.sql(f"DROP TABLE IF EXISTS onelake.dbo.{t}")
          c.sql(f"""CREATE TABLE onelake.dbo.{t} AS
                    SELECT ['AAA','BBB','CCC','DDD'][(i % 4) + 1] AS duid, i AS mw
                    FROM range(1000000) t(i)""")
          n = c.sql(f"SELECT count(*) FROM onelake.dbo.{t}").fetchone()[0]
          if n != 1000000:
              print(f"::error::round-trip read back {n} rows, expected 1000000")
              sys.exit(1)
          print(f"round-trip: {n} rows")
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a") as fh:
              print("| OneLake Iceberg REST attach | ok |", file=fh)
              print(f"| iceberg round-trip | {n:,} rows |", file=fh)
          PY

      # THE STEP THAT ANSWERS THE QUESTION. The one above proves the catalog works; this reads the
      # footer of the parquet the ICEBERG writer just produced and asserts the core writer's
      # encoding_stats survived that path. A red here with a green "local parquet" step is the
      # interesting failure and belongs in a duckdb-iceberg issue.
      #
      # The REST catalog hands back no file list, hence the glob over the direct abfss path
      # `provision.py smoke` printed. The listing is printed first and unfiltered: if OneLake ever
      # lays an iceberg table out differently, that shows as an empty list rather than as a
      # mysterious assertion failure.
      #
      # **TWO PATTERNS, AND `**/*.parquet` IS ILLEGAL HERE.** The abfss filesystem refuses a
      # recursive wildcard anywhere but the END — run 32788966089 died on
      #   Not implemented Error: abfss do not manage recursive lookup patterns … only pattern
      #   ending by ** are allowed
      # so the LISTING uses a trailing `**` (which is legal, and shows the whole table directory
      # whatever its shape) while the footer read uses the literal `data/*.parquet` that run
      # 32788719423 proved is where OneLake puts an iceberg table's parquet. Handing the trailing
      # `**` to parquet_metadata instead would drag in the `metadata/` side and fail on the first
      # non-parquet file.
      - name: Iceberg-written parquet carries encoding_stats
        if: inputs.onelake
        run: |
          set -euo pipefail
          python - <<'PY'
          import os, sys, duckdb
          c = duckdb.connect()
          c.sql("LOAD azure")
          c.sql("SET azure_transport_option_type = 'curl'")
          c.execute(f"CREATE SECRET onelake_storage (TYPE azure, PROVIDER access_token, "
                    f"ACCESS_TOKEN '{os.environ['ONELAKE_TOKEN']}')")
          base = f"{os.environ['SMOKE_TABLE_PATH']}/dbo/{os.environ['SMOKE_TABLE']}"
          print("table directory:", c.sql(f"SELECT file FROM glob('{base}/**')").fetchall())
          n = c.sql(f"""SELECT count(*) FROM parquet_metadata('{base}/data/*.parquet')
                        WHERE path_in_schema = 'duid'
                          AND len(list_filter(encoding_stats,
                                              lambda x: x.page_type = 'DICTIONARY_PAGE')) > 0""").fetchone()[0]
          if n == 0:
              print("::error::iceberg-written parquet carries no DICTIONARY_PAGE encoding_stats for duid")
              sys.exit(1)
          print(f"iceberg: {n} dictionary-encoded chunk(s)")
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a") as fh:
              print("| iceberg parquet encoding_stats | present |", file=fh)
          PY

      # THE STEP THAT ANSWERS "CAN THE LEG PASS A ROW GROUP SIZE". dbt-duckdb exposes no writer
      # config, so `COPY`'s ROW_GROUP_SIZE is unreachable from the iceberg leg — but duckdb-iceberg
      # reads the geometry off ICEBERG TABLE PROPERTIES instead, and `CREATE TABLE … WITH (…)` is
      # where they are set. That syntax parses in DuckDB and is refused only by the DUCKDB catalog
      # (`Catalog Error: WITH clause is not supported for tables in a duckdb catalog`), so whether
      # it survives the OneLake REST catalog is an empirical question, and this is the cheap place
      # to ask it — one free runner, one throwaway lakehouse, no Fabric capacity.
      #
      # `write.parquet.row-group-size` is ROWS — duckdb-iceberg's own extension, the unit duckrun's
      # `row_group_size` dispatch input already carries, which is what lets one number drive both
      # DuckDB writers. ⚠️ IT IS NOT ENOUGH ON ITS OWN, and that is the whole shape of this step:
      # `iceberg_insert.hpp:69` hardcodes `batch_size_bytes` to 128 MB, both thresholds are live,
      # and the first to hit wins — so on any table wide enough to reach 128 MB (UNCOMPRESSED)
      # before it reaches x rows, the row count is advisory. `iceberg_geometry()` therefore emits
      # the PAIR, and this probes the pair plus a control.
      #
      # TWO WRITES OF ONE WIDE TABLE, and the control is the half that would have caught run
      # 33733500776 before it cost a dispatch:
      #   control  — rows ALONE     — must produce MORE row groups than the shipping case, i.e. the
      #                               byte default binds and the row count is ignored. A green here
      #                               with the OLD narrow table is exactly what happened.
      #   shipping — rows + 1 GiB   — must produce exactly TOTAL/ROWS, i.e. the row count binds.
      # Neither is the round-trip table above: a refused property must fail THIS step and leave the
      # encoding_stats proof standing, or one result masks the other. And `if: always()` for the
      # same reason in the other direction — a failure above must not silently skip this answer.
      #
      # ⚠️ The ATTACH options are `stage_create_tables 0` / `skip_create_table_metadata_updates 1`,
      # copied from the leg. If the property is dropped rather than refused, suspect those first —
      # they are what make this catalog's CREATE path unlike a stock one.
      - name: An Iceberg table property sets the row group size
        if: always() && inputs.onelake
        run: |
          set -euo pipefail
          python - <<'PY'
          import os, sys, math, duckdb
          rows, want = int(os.environ["SMOKE_RG_ROWS"]), int(os.environ["SMOKE_RG_EXPECTED"])
          cols, total = int(os.environ["SMOKE_RG_COLS"]), int(os.environ["SMOKE_RG_TOTAL"])
          budget = int(os.environ["SMOKE_RG_BUDGET_MB"])
          assert want == math.ceil(total / rows), "SMOKE_RG_EXPECTED disagrees with TOTAL/ROWS"
          c = duckdb.connect()
          c.sql("LOAD iceberg; LOAD azure")
          c.sql("SET azure_transport_option_type = 'curl'")
          tok, ws, ep = (os.environ["ONELAKE_TOKEN"], os.environ["WAREHOUSE_PATH"],
                         os.environ["ONELAKE_ENDPOINT"])
          c.execute(f"CREATE SECRET onelake_storage "
                    f"(TYPE azure, PROVIDER access_token, ACCESS_TOKEN '{tok}')")
          c.execute(f"""ATTACH '{ws}' AS onelake (
                          TYPE iceberg, endpoint '{ep}', token '{tok}',
                          access_delegation_mode 'none', stage_create_tables 0,
                          skip_create_table_metadata_updates 1, default_schema 'dbo')""")
          # 50 BIGINT columns, so a row group of `rows` rows cannot fit in the writer's 128 MB.
          sel = ", ".join(f"i + {n} AS c{n}" for n in range(cols))
          body = f"SELECT {sel} FROM range({total}) t(i)"

          def written(tbl, props):
              """CREATE the table with `props`, return its total row-group count."""
              c.sql(f"DROP TABLE IF EXISTS onelake.dbo.{tbl}")
              # The clause the macro emits, in the position and the sorted order it emits it.
              clause = ", ".join(f"'{k}' = '{v}'" for k, v in sorted(props.items()))
              c.sql(f"CREATE TABLE onelake.dbo.{tbl} WITH ({clause}) AS {body}")
              try:
                  print(f"  {tbl} properties:",
                        c.sql(f"SELECT * FROM iceberg_table_properties('onelake.dbo.{tbl}')").fetchall())
              except Exception as exc:                 # a table function's signature may move
                  print(f"  {tbl} properties: unreadable —", exc)
              # sum(num_row_groups) over the FILES, not count(DISTINCT row_group_id): the id
              # restarts per file, so the distinct count undercounts the moment there are two.
              g = f"{os.environ['SMOKE_TABLE_PATH']}/dbo/{tbl}/data/*.parquet"
              n = c.sql(f"SELECT sum(num_row_groups) FROM parquet_file_metadata('{g}')").fetchone()[0]
              mx = c.sql(f"SELECT max(row_group_num_rows) FROM parquet_metadata('{g}')").fetchone()[0]
              print(f"  {tbl}: {n} row groups, max {mx:,} rows/group")
              return int(n)

          print(f"{cols} cols x {total:,} rows, asking {rows:,} rows/row-group")
          ship = written(os.environ["SMOKE_TABLE_RG"],
                         {"write.parquet.row-group-size": rows,
                          "write.parquet.row-group-size-bytes": budget * 1048576})
          ctl = written(os.environ["SMOKE_TABLE_RG_CTL"],
                        {"write.parquet.row-group-size": rows})

          # The pair binds on ROWS: exactly total/rows groups.
          ok_ship = ship == want
          # The control binds on BYTES: strictly more groups, the row count ignored. This is the
          # assertion the old narrow-table probe could not make, and the reason it read green
          # while the nyc leg moved nothing.
          ok_ctl = ctl > want
          for label, got, expect, ok in (
                  (f"`row-group-size` + `-bytes` = {budget} MB", ship, f"exactly {want}", ok_ship),
                  ("`row-group-size` alone (control)", ctl, f"more than {want}", ok_ctl)):
              verdict = "honoured" if ok else "**UNEXPECTED**"
              print(f"{label}: {got} row groups, expected {expect} — {verdict}")
              with open(os.environ["GITHUB_STEP_SUMMARY"], "a") as fh:
                  print(f"| iceberg {label} | {got} row groups, expected {expect}, {verdict} |", file=fh)
          if not ok_ctl:
              print("the control did not go byte-bound — the table is too NARROW for a 128 MB "
                    "budget to bind, so this probe cannot see an inert row-count property. "
                    "Raise SMOKE_RG_COLS or SMOKE_RG_ROWS.", file=sys.stderr)
          sys.exit(0 if (ok_ship and ok_ctl) else 1)
          PY

      # ---------------------------------------------------------------- always
      # `always()`, and it deletes BY GUID from the record `ensure()` wrote — so a failure anywhere
      # above still removes the lakehouse rather than leaving it drawing background CU into the next
      # dispatch's measurement window. Exits non-zero if anything survived, same as benchmark.yml's.
      - name: Teardown
        if: always() && inputs.onelake
        run: |
          if [ -f "$RUN_RECORD" ]; then
            python .github/scripts/provision.py teardown "$RUN_RECORD"
          else
            echo "no run record — nothing was provisioned"
          fi
